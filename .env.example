# Copy to .env and fill in locally (never commit .env)
OPENAI_API_KEY=sk-your-openai-key-here

# Optional: HISIS Oracle via SSH tunnel (see docs/GIT_SETUP.md)
# ORA_KRHIP_HOST=127.0.0.1
# ORA_KRHIP_PORT=15211

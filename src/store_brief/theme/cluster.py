"""Promote recurring free-form theme labels into stable themes. Not needed for prototype.

Later: embed theme labels, cluster (e.g. HDBSCAN), name clusters with the LLM, and persist a
theme registry so labels stabilize across days.
"""
from __future__ import annotations


def cluster_themes(events):  # placeholder
    raise NotImplementedError("theme clustering is a post-prototype step")
